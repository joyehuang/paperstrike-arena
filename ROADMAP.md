# Paperstrike 后续玩法方案

这些是待讨论的范围建议，本次未实现多人联网。

## 优先增加单局变化

1. 占点：地图上轮换一个得分区域，鼓励移动、侧翼绕行和抢补给，减少在同一位置反复交火。
2. 武器轮换挑战：每完成三次击杀换一把枪，四种武器全部完成即获胜；复用现有枪械，形成明确的短局目标。
3. 难度选项：独立控制敌人人数、射击间隔和命中率，让玩家能选择喜欢的地图而不被迫接受更高难度。
4. 局后统计：准确率、爆头、每把武器的击杀和死亡热区，让下一局有具体的练习目标。

建议先选占点或武器轮换其中一个验证，不同时扩展多个模式。

## 多人第一版建议

- 2–4 人房间码邀请，自由混战，先只用废稿堆场；保留四把枪和补给。
- 昵称、准备状态、房主开局、3 分钟计时、击杀排行、断线重连和重新开局。
- 暂不加入排位、语音、账号等级和复杂匹配；先把移动、命中和复活的一致性做好。
- 房间人数不足时，AI 补位作为第二阶段功能，避免第一版同时处理联网与 AI 接管。

## 实现边界与验证

浏览器保留画面、输入和即时视觉反馈；房间服务统一裁定移动边界、射击间隔、弹药、伤害、补给与复活。客户端上传输入，不直接提交有效伤害或击杀数。

先将当前 Arena 中的规则与 Three.js 展示拆开，以约 30 Hz 的服务端模拟作为原型起点，再根据实测调整；本地预测自己的移动、插值其他玩家，并对射击做有上限的延迟补偿。

前端继续在 Vercel。房间服务的托管方式需要先验证长连接持续时间、同一房间的状态归属、地区延迟和并发成本，再决定是否与前端共用平台，不在本轮预先购买服务。

验收覆盖两台设备、30/100/200 ms 延迟、抖动、断线、房主离开、同时拾取道具、同时击杀，以及篡改客户端血量和开火速度。保持原单人入口可独立运行。
